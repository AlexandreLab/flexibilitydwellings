

poetry is used on mac to manage the virtual environment and libraries, however on windows this does not work well.

A virtual environment is created using with [Virtual Environment Name] = flexibility_model:
python3 -m venv [Virtual Environment Name]
.\[Virtual Environment Folder Name]\Scripts\activate

to deactivate:
(sample_venv) C:\Users\yoges\Python_venv> deactivate

libraries to install:

to install geopandas:
pip install pipwin
pipwin install gdal
pipwin install fiona
pip install geopandas

seaborn
matplotlib
pandas
numpy
scipy
sklearn
geopandas

